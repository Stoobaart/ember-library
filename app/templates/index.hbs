<div class='container'>
  
  <video class="vid" autoplay loop width="100%" src="/assets/videos/Flicking_Through_Book.mov"></video>
  <h1 class="welcomeMsg">Welcome to your library</h1>
  <div class="jumbotron text-center align-jumbotron">
    <h1>It's party time!</h1>

    <br/><br/>

    <p>Don't miss our launch party, request an invitation now.</p>

    <div class="form-horizontal form-group form-group-lg row">
      <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
        {{input type="email" value=model.email class="form-control" placeholder="Please enter e-mail address."}}
      </div>
      <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4 col-md-3">
        {{#if model.isDisabled}}
        {{else}}
          <button class="btn btn-primary btn-lg btn-block" {{action 'saveInvitation' model}}>Request invitation</button>
        {{/if}}
      </div>
    </div>

    {{#if responseMessage}}
      <div class="alert alert-success">{{responseMessage}}</div>
    {{/if}}

    <br/><br/>
  </div>
  <img class="homeImage" src="/assets/images/image.jpg" width='100%'>
  <h3 class="home-msg">So many books, so little time! Maximise your personal reading time by finding the books you want, where you want, now!</h3>
</div>